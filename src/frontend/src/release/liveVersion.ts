/**
 * Single source of truth for the current live publish target.
 * This constant indicates which draft version is currently deployed as the live application.
 */
export const LIVE_VERSION = 'Draft 34';
export const LIVE_VERSION_DATE = '2026-02-06';
